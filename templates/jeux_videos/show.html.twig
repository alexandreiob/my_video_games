{% extends 'base.html.twig' %}

{% block title %}JeuxVideos{% endblock %}

{% block body %}
    <h1>JeuxVideos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jeux_video.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ jeux_video.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ jeux_video.type }}</td>
            </tr>
            <tr>
                <th>Released</th>
                <td>{{ jeux_video.released ? jeux_video.released|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ jeux_video.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_jeux_videos_index') }}">back to list</a>

    <a href="{{ path('app_jeux_videos_edit', {'id': jeux_video.id}) }}">edit</a>

    {{ include('jeux_videos/_delete_form.html.twig') }}
{% endblock %}
